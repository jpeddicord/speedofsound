<resources>

	<style name="AppBaseTheme" parent="android:Theme" />

	<style name="AppTheme" parent="AppBaseTheme" />

	<style name="FallbackDialog" parent="@android:style/Theme.Dialog" />

</resources>
